# 📋 خطة التطوير والتنفيذ الهندسية الشاملة: مطابقة عمل ومسميات أزرار "تصميم 7" (Design 7 POS)
> **المشروع**: AN POS | **المكون المستهدف**: `src/features/pos/components/design7/` وكافة مكوناته  
> **المرجع الهندسي**: `POS_DESIGN_7_ANALYSIS.md` | معايير `ISO 9241-410 Ergonomics & Touch Sizing` | قانون فيتس `Fitts's Law` | السلامة من أخطاء الإدخال `Poka-Yoke`  
> **تاريخ الاعتماد**: سبتمبر 2026 | **الحالة**: خطة هندسية جاهزة للتنفيذ بعد مراجعة المستخدم  

---

## 📌 الفهرس التنفيذي

1. [الأهداف الهندسية والتشغيلية للخطة](#1-الأهداف-الهندسية-والتشغيلية-للخطة)
2. [المكونات المستهدفة ونطاق التعديل البرمجي](#2-المكونات-المستهدفة-ونطاق-التعديل-البرمجي)
3. [مصفوفة التعديلات الدقيقة لكل مكون](#3-مصفوفة-التعديلات-الدقيقة-لكل-مكون)
   * [3.1 شريط الأوامر العلوي (`Design7TopRibbon.tsx`)](#31-شريط-الأوامر-العلوي-design7topribbontsx)
   * [3.2 شريط المسح والحالة الفورية (`Design7ActiveScanStrip.tsx`)](#32-شريط-المسح-والحالة-الفورية-design7activescanstriptsx)
   * [3.3 مصفوفة أزرار التحكم اللمسية 3×5 (`Design7ActionKeypad.tsx`)](#33-مصفوفة-أزرار-التحكم-اللمسية-35-design7actionkeypadtsx)
   * [3.4 شبكة الفئات والمفضلة السريعة (`Design7BottomFavoritesPad.tsx`)](#34-شبكة-الفئات-والمفضلة-السريعة-design7bottomfavoritespadtsx)
   * [3.5 جدول السلة والعمود المالي (`Design7BasketTable.tsx` & `Design7FinancialSidebar.tsx`)](#35-جدول-السلة-والعمود-المالي)
4. [مصفوفة اختصارات لوحة المفاتيح الموحدة (F1 - F12)](#4-مصفوفة-اختصارات-لوحة-المفاتيح-الموحدة-f1---f12)
5. [خطة الاختبار والتحقق الهندسي (Verification & Testing Plan)](#5-خطة-الاختبار-والتحقق-الهندسي-verification--testing-plan)

---

## 1. الأهداف الهندسية والتشغيلية للخطة

تستهدف هذه الخطة سد الفجوة بين المسميات الظاهرة والوظائف البرمجية الفعلية في **تصميم 7** بنسبة **100%**، مع الالتزام بالمعايير التالية:
* **تصفير التسميات المشوهة والمضللة**: القضاء النهائي على مصطلحات OCR المشوهة مثل *"سجلات فن الاحتياط"*، *"المستمر"*، و *"قائمة المشتروات"*.
* **إنهاء أزمة الأزرار الصامتة**: تزويد جميع أزرار لوحة التحكم الـ 15 بنصوص عربية صريحة وأيقونات دلالية معيارية من `Lucide SVG`.
* **العزل اللوني والأمان التشغيلي (Poka-Yoke)**: تمييز العمليات التدميرية (حذف السطر، تفريغ السلة) باللون الأحمر التحذيري وفصلها عن أزرار التأكيد الخضراء والزرقاء.
* **التوافق التام مع شاشات اللمس (Touch Target Ergonomics)**: ضمان مساحات لمسية تفاعلية مريحة تتوافق مع معايير `ISO 9241` ($\ge 42\text{px}$).

---

## 2. المكونات المستهدفة ونطاق التعديل البرمجي

```
src/features/pos/components/design7/
├── components/
│   ├── Design7TopRibbon.tsx              # تصحيح مسميات شريط الأوامر العلوي وإلغاء الازدواجية
│   ├── Design7ActiveScanStrip.tsx        # تصحيح شارة السعر من "المستمر" إلى "سعر الوحدة"
│   ├── Design7ActionKeypad.tsx           # إعادة بناء مصفوفة 3×5 بنصوص عربية وأيقونات دقيقة
│   ├── Design7BottomFavoritesPad.tsx     # استبدال نصوص FAV بديناميكية التصنيفات الحقيقية
│   ├── Design7BasketTable.tsx            # تحسين إشارات التحكم وأزرار الكمية والحذف
│   └── Design7FinancialSidebar.tsx       # ضبط وتأكيد مسميات المدفوع والفكة والتسوية
├── hooks/
│   └── useDesign7Shortcuts.ts            # مواءمة معالجة الاختصارات مع مصفوفة F1-F12 الموحدة
└── __tests__/
    └── Design7POSLayout.test.tsx         # توسيع اختبارات الوحدة لتغطية كافة المسميات الجديدة
```

---

## 3. مصفوفة التعديلات الدقيقة لكل مكون

### 3.1 شريط الأوامر العلوي (`Design7TopRibbon.tsx`)

| الزر الحالي | المشكلة والخلل | التعديل البرمجي المعتمد | المسمى الجديد المصحح | الأيقونة والاختصار |
| :--- | :--- | :--- | :--- | :--- |
| **"خروج"** | يوحي بإغلاق البرنامج | التوجيه للوحة التحكم | **الرئيسية** | `ArrowRight` أو `Home` (Esc) |
| **"المبيعات"** | غامض ومبهم | فتح سجل المبيعات | **سجل المبيعات والمرتجع** | `History` (F2) |
| **"قائمة المشتروات"** ❌ | **تناقض تام**: يستدعي `onSaveAsOrder` لحفظ طلبية زبون! | تصحيح المسمى | **حفظ كطلبيّة زبون** | `FileCheck` (F4) |
| **"سجلات فن الاحتياط"** ❌ | **تشويه OCR**: يستدعي `onOpenSuspended`! | تصحيح المسمى | **الفواتير المعلقة (المسودات)** | `PauseCircle` (F8) |
| **"ON / OFF"** ⚠️ | غامض مع أيقونة طابعة | توضيح الوظيفة وحالة الزر | **طباعة تلقائية: مفعلة/معطلة** | `Printer` (F7) مع بادج لوني |
| **"طباعة سريعة"** | مكرر ومطابق لطباعة السند | تخصيصه للدفع الفوري | **دفع نقدي فوري** | `Zap` / `Banknote` (F1) |
| **"طباعة السند"** | تسوية سلة وإصدار فاتورة | ضبط المسمى | **تأكيد وتسوية الفاتورة** | `Receipt` (Enter / F1) |
| **"تغيير السند"** ❌ | **تناقض**: يفتح نافذة الخصم المالي! | تصحيح المسمى | **تخفيض الفاتورة** | `Edit3` / `Percent` (F6) |
| **"سند جديد"** | تفريغ السلة الحالية | إضافة تحذير لمنع مسح السلع دون قصد | **إلغاء وتفريغ السلة** | `Trash2` (Ctrl+Del) |

---

### 3.2 شريط المسح والحالة الفورية (`Design7ActiveScanStrip.tsx`)

1. **تصحيح شارة السعر**:
   - استبدال الكلمة المشوهة `"المستمر"` بـ **`"سعر الوحدة"`** أو **`"السعر"`**.
   - ربط القيمة بـ `activeItem.unitPrice` بصيغة العملة المعتمدة.
2. **شريط الباركود اللمسي**:
   - إبراز اختصار `F3` للانتقال السريع لحقل الباركود، مع توضيح نص التلميح: *"امسح الباركود أو أدخله واضغط Enter"*.

---

### 3.3 مصفوفة أزرار التحكم اللمسية 3×5 (`Design7ActionKeypad.tsx`)

إعادة هيكلة كل زر في الشبكة ليحتوي على:
- ارتفاع $\ge 42\text{px}$ مع دعم اللمس المباشر.
- أيقونة معيارية من مكتبة `lucide-react`.
- نص عربي مصغر أسفل الأيقونة بحجم `text-[9px] font-bold`.

#### جدول الأزرار الـ 15 المحدث:

```
┌──────────────────────────┬──────────────────────────┬──────────────────────────┐
│ صف 1: الزبون (F9)        │ صف 1: بحث السلع (F10)    │ صف 1: تفاصيل المادة      │
│ [أيقونة: UserCheck]     │ [أيقونة: Search]         │ [أيقونة: Info]           │
├──────────────────────────┼──────────────────────────┼──────────────────────────┤
│ صف 2: حذف السطر (Del)    │ صف 2: السطر السابق (↑)   │ صف 2: صنف حر / يدوي (F8) │
│ [أيقونة: Trash2 - أحمر]  │ [أيقونة: ArrowUp]        │ [أيقونة: Sparkles]       │
├──────────────────────────┼──────────────────────────┼──────────────────────────┤
│ صف 3: إنقاص الكمية (-)   │ صف 3: تأكيد ودفع (Enter) │ صف 3: زيادة الكمية (+)   │
│ [أيقونة: Minus]          │ [أيقونة: Check - أخضر]   │ [أيقونة: Plus]           │
├──────────────────────────┼──────────────────────────┼──────────────────────────┤
│ صف 4: تسوية الفاتورة     │ صف 4: السطر التالي (↓)   │ صف 4: تعديل السعر        │
│ [أيقونة: Receipt]        │ [أيقونة: ArrowDown]      │ [أيقونة: Tag - بديل الصورة]│
├──────────────────────────┼──────────────────────────┼──────────────────────────┤
│ صف 5: سجل الفواتير       │ صف 5: آلة حاسبة لمسية    │ صف 5: كيبورد افتراضي     │
│ [أيقونة: History]        │ [أيقونة: Calculator]     │ [أيقونة: Keyboard]       │
└──────────────────────────┴──────────────────────────┴──────────────────────────┘
```

---

### 3.4 شبكة الفئات والمفضلة السريعة (`Design7BottomFavoritesPad.tsx`)

1. **استبدال Placeholder الفئات**:
   - إزالة نصوص `FAV1 .. FAV16` و `حبوب فرات`.
   - عرض أسماء التصنيفات الحقيقية المستلمة من `categories` (مثال: مواد غذائية، مشروبات، معلبات، منظفات...).
   - إخفاء أو تعطيل التبويبات الفائضة عن عدد فئات المتجر الفعلية لتفادي أزرار وهمية ميتة.
2. **أزرار المنتجات السريعة (4×4)**:
   - المحافظة على التنسيق المزدوج (اسم المنتج + السعر الفوسفوري).
   - توفير تأثير النقر المباشر للإضافة الفورية إلى السلة بلمسة واحدة.

---

### 3.5 جدول السلة والعمود المالي

1. **جدول السلة (`Design7BasketTable.tsx`)**:
   - تكبير مناطق اللمس لأزرار الكمية `+` و `-` إلى `26×26px`.
   - زر حذف السطر مزود بأيقونة `Trash2` وتباين لوني تحذيري يمنع الضغط العفوي.
2. **العمود المالي (`Design7FinancialSidebar.tsx`)**:
   - فحص شاشات المدفوع، الفكة المتبقية، وأزرار الفئات السريعة (`المبلغ التام`، `1000`، `2000`).
   - زر الإجمالي الفوسفوري يعمل كمنطقة ضغط عريضة تستدعي `onSettleSale`.

---

## 4. مصفوفة اختصارات لوحة المفاتيح الموحدة (F1 - F12)

| المفتاح | الوظيفة المعتمدة | المكون المعني | الإجراء البرمجي |
| :-: | :--- | :--- | :--- |
| **F1** | تأكيد وتسوية الفاتورة | `Design7TopRibbon` / `FinancialSidebar` | `onSettleSale()` |
| **F2** | سجل المبيعات والمرتجع | `Design7TopRibbon` / `ActionKeypad` | `onOpenSalesHistory()` |
| **F3** | التركيز على حقل الباركود | `Design7ActiveScanStrip` | Focus Barcode Input |
| **F4** | حفظ السلة كطلبيّة زبون | `Design7TopRibbon` | `onSaveAsOrder()` |
| **F6** | تخفيض الفاتورة (الخصم) | `Design7TopRibbon` / `FinancialSidebar` | `onOpenDiscount()` |
| **F7** | تبديل الطباعة التلقائية | `Design7TopRibbon` | `onToggleAutoPrint()` |
| **F8** | الفواتير المعلقة / صنف حر | `Design7TopRibbon` / `ActionKeypad` | `onOpenSuspended()` / `Free` |
| **F9** | تحديد أو تغيير الزبون | `Design7ActionKeypad` / `CustomerBanner`| `onSelectCustomer()` |
| **F10**| بحث واستعراض السلع | `Design7ActionKeypad` | `onOpenSearch()` |
| **F11**| ملء الشاشة وتكبير العرض | شريط النظام | `toggleFullscreen()` |
| **Del**| حذف الصنف النشط من السلة | `Design7BasketTable` / `ActionKeypad` | `onRemoveFromCart()` |
| **Esc**| الرجوع للوحة التحكم الرئيسية| `Design7TopRibbon` | `onNavigateBack()` |

---

## 5. خطة الاختبار والتحقق الهندسي (Verification & Testing Plan)

### 5.1 الفحص الآلي واختبارات الوحدة (Automated Tests)
* تشغيل اختبارات `Design7POSLayout.test.tsx` للتأكد من:
  1. ظهور النصوص المصححة في الواجهة (`سعر الوحدة`، `حفظ كطلبيّة`، `الفواتير المعلقة`، `تخفيض الفاتورة`).
  2. استجابة أزرار لوحة التحكم 3×5 وتفعيل التوابع البرمجية المقابلة لكل زر.
  3. عدم وجود أي استدعاء مفقود أو خطأ في الأنواع (TypeScript clean compile).

```bash
npm test src/features/pos/components/__tests__/Design7POSLayout.test.tsx
```

### 5.2 الفحص البصري التفاعلي (Interactive Touch Verification)
* التحقق من وضوح النصوص والأيقونات على مقاسات الشاشات التجارية ($1024\times 768$ و $1366\times 768$ و $1920\times 1080$).
* تجربة مسح الباركود، تعديل الكمية، الخصم، الدفع، وحفظ الطلبية للتأكد من انسيابية تجربة الكاشير.
