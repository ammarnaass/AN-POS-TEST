# المعمارية المدمجة (سيرفر محلي + Cloud) لنظام نقاط البيع AN POS
## دليل التصميم والتنفيذ الموزع (Local Server + Client PC + Cloud Architecture)

---

## 1. نظرة عامة والهدف الهندسي (Executive Summary)

تهدف هذه المعمارية إلى تمكين نظام **AN POS** من العمل بنمط **الشبكة الهجينة (Hybrid Multi-Tier Topology)** بحيث:
1. **كمبيوتر الخادم (Local Server / Primary POS):**
   - يحتفظ بقاعدة البيانات المركزية على مستوى المتجر (SQLite WAL).
   - يشغل محرك خادم واجهات البرمجة السريعة (Fastify REST + WebSockets) على الشبكة المحلية LAN.
   - يتولى البث والاكتشاف التلقائي (Bonjour mDNS / UDP Discovery).
   - يتولى المزامنة الثنائية مع السحابة (Cloud Engine) في الخلفية عند توفر الإنترنت.
2. **كمبيوتر العميل (Client PC / Secondary Terminal):**
   - يعمل كنقطة بيع مستقلة بالكامل وسريعة الاستجابة (أقل من 5ms).
   - يكتشف الخادم في الشبكة المحلية تلقائياً ويقترن به بأمان.
   - يدير شاشته وطابعاته وأجهزته الطرفية (طابعة الفواتير، قارئ الباركود، درج الكاش، شاشة العميل) محلياً.
   - يتمتع بقدرة **Offline-First Resilience**: في حال انقطاع الشبكة أو إغلاق الخادم مؤقتاً، يستمر في البيع عبر طابور محلي (Outbox Queue) دون توقف.
3. **السحابة المركزية (Cloud Sync Hub):**
   - تجميع بيانات المبيعات والمخزون للفروع والمتاجر المتعددة.
   - إمكانية الإشراف عن بعد عبر الويب أو تطبيقات الهاتف.
   - نسخ احتياطي سحابي مشفر تراكمي ضد كوارث تلف الأجهزة الفيزيائية.

---

## 2. الطبولوجيا ومخطط تدفق البيانات (Network Topology)

```
       +-------------------------------------------------------------+
       |                     السحابة المركزية                        |
       |                (AN POS Cloud Sync Engine)                   |
       |             [Postgres / Cloud REST API / Auth]              |
       +-------------------------------------------------------------+
                                      ▲
                                      │  (HTTPS / TLS Sync Worker)
                                      ▼
+---------------------------------------------------------------------------------+
|                       الشبكة المحلية للمتجر (Store LAN)                         |
|                                                                                 |
|   +------------------------------------+                                        |
|   |   كمبيوتر الخادم الرئيسي (Server PC)  |                                        |
|   |   - IP: 192.168.1.100 (Port 3000)   |                                        |
|   |   - SQLite WAL (Master Database)   |                                        |
|   |   - Fastify HTTP & WebSocket Engine|                                        |
|   |   - Bonjour & UDP Broadcast Beacon |                                        |
|   |   - Cloud Outbox & Inbox Worker    |                                        |
|   +------------------------------------+                                        |
|                     ▲                                                           |
|                     │  (HTTP REST + WebSocket Realtime via LAN)                  |
|                     ▼                                                           |
|   +------------------------------------+    +-------------------------------+   |
|   |    كمبيوتر العميل (Client PC)      |    |  تطبيقات الهواتف (Mobile POS) |   |
|   |    - كاشير فرعي 1 (Terminal #2)    |    |  - بائع الصالة / الجرد المتنقل|   |
|   |    - طابعة فواتير ودرج محلي        |    |  - مسح باركود بالكاميرا       |   |
|   |    - كاش محلي + Offline Queue      |    +-------------------------------+   |
|   +------------------------------------+                                        |
+---------------------------------------------------------------------------------+
```

---

## 3. توزيع المسؤوليات بين الحواسيب (Separation of Responsibilities)

| الميزة / الوظيفة | كمبيوتر الخادم (Server PC) | كمبيوتر العميل (Client PC) | السحابة المركزية (Cloud) |
| :--- | :--- | :--- | :--- |
| **قاعدة البيانات الأساسية** | المصدر المركزي للمتجر (Master SQLite) | كاش محلي للقراءة + طابور Outbox | قاعدة بيانات مركزية مجمّعة للمؤسسة |
| **إدارة المنتجات والتسعير** | تعديل كامل وإرسال التحديث للعملاء | قراءة فورية مع استقبال البث المباشر | إدارة عامة مركزية للفروع |
| **إنشاء وتمرير المبيعات** | يدعم البيع المباشر ككاشير رئيسي | بيع سريع محلي + إرسال فوري للخادم | استقبال الفواتير واستخراج التقارير |
| **الطباعة ودرج النقود** | أجهزة الطباعة الموصولة بالخادم | أجهزة الطباعة الموصولة بالعميل محلياً | تقارير رقمية وإيصالات إلكترونية |
| **الاتصال بالإنترنت** | إلزامي للمزامنة السحابية فقط (اختياري للبيع) | غير مطلوب إطلاقاً (يعمل على LAN فقط) | تتطلب اتصال الإنترنت |
| **مواجهة انقطاع LAN** | يستمر في البيع كجهاز مستقل | ينتقل إلى Offline Outbox مؤقتاً | لا تتأثر، وتنتظر عودة الاتصال |

---

## 4. استراتيجية مزامنة البيانات وحسم النزاعات (Data Synchronization & Conflict Resolution)

### 4.1 الترقيم الذكي الموزع للفواتير (Distributed Unique Invoicing)
لمنع تضارب أرقام الفواتير الرسمية عند بيع كاشير الخادم وكاشير العميل في نفس اللحظة:
- يتم إعطاء كل طرف رمز محطة فريد:
  - الخادم: `T1` (Terminal 01)
  - العميل الأول: `T2` (Terminal 02)
  - العميل الثاني: `T3` (Terminal 03)
- تركيبة رقم الفاتورة: `INV-[YYMMDD]-[TerminalId]-[Sequence]`
  - مثال الخادم: `INV-260924-T1-0001`
  - مثال العميل: `INV-260924-T2-0001`
- يتم استخدام `UUIDv4` للمعرفات البرمجية (`id`) لجميع السجلات لضمان عدم حدوث تصادم نهائياً.

### 4.2 حركة المخزون التراكمية (Ledger-Based Atomic Stock Decrement)
- بدلاً من تحديث المخزون بقيمة مطلقة (`quantity = 50`)، يتم تسجيل كل عملية بيع كحركة مخزنية متجهة (`movement_type = 'out', delta = -2`).
- يضمن الخادم تطبيق الحركات بتسلسل خطي متناسق، مما يمنع تجاوز أو فقدان أي كمية.

### 4.3 حسم النزاعات (Conflict Resolution Policies)
1. **البيانات التعريفية (المنتجات، الفئات، الأسعار، المستخدمين):**
   - سياسة **Master-Wins**: الخادم المحلي والسحابة هما المرجع الرئيسي. يتم استبدال الكاش القديم لدى العميل فوراً.
2. **المعاملات المالية والفواتير (Sales, Payments, Cash Sessions):**
   - سياسة **Append-Only Immutable Ledger**: كل فاتورة صادرة من أي جهاز هي معاملة مالية معتمدة تُدمج تراكمياً ولا يتم حذفها إطلاقاً.
3. **الجلسات وصندوق الكاشير (Cash Register Shifts):**
   - كل جهاز (سواء خادم أو عميل) يفتح جلسة صندوق خاصة به مستندة لرمز الجهاز (`device_id`) والمستخدم الحالي، مما يمنع تداخل أرصدة الأدراج.

---

## 5. خطة التنفيذ المفصلة مقسمة على مراحل (Phased Execution Plan)

### المرحلة 1: معمارية تجريد النقل وتبديل نمط التشغيل (Data Transport Abstraction & Role Selection)
* **الهدف:** تمكين كود الواجهة (React Frontend) من طلب البيانات دون معرفة ما إذا كان ينفذ استعلام SQLite محلي أو يتصل بـ HTTP Server.
* **الخطوات التفصيلية:**
  1. إنشاء طبقة موحدة `TransportGateway` في الواجهة:
     - إذا كان الوضع `Server` أو `Single`: تمرير الطلبات إلى `window.electronAPI.db.*` (IPC المحلي).
     - إذا كان الوضع `Client`: تمرير الطلبات تلقائياً عبر `ApiClient` مبني بـ Fetch/Axios إلى `http://[SERVER_IP]:3000/api/*` مع رأس `x-session-token` و `x-device-id`.
  2. توسيع جدول الإعدادات `settings` بحقول:
     - `terminal_role`: `'server' | 'client'`.
     - `server_lan_url`: عنوان الخادم المعتمد للعميل (مثال: `http://192.168.1.100:3000`).
     - `terminal_code`: بادئة الترقيم (مثال: `T02`).
  3. تحديث شاشة [NetworkTab.tsx](file:///home/ammar/AN-POS-TEST/src/features/settings/tabs/NetworkTab.tsx) لإتاحة التبديل السهل بين "خادم رئيسي (Server Master)" و "جهاز عميل (Client Terminal)".

---

### المرحلة 2: الاكتشاف التلقائي والاقتران المشفر (Zero-Config Discovery & Pairing)
* **الهدف:** ربط حاسوب العميل بحاسوب الخادم خلال 10 ثوانٍ دون الحاجة لمعرفة عنوان IP أو إعدادات الشبكة المعقدة.
* **الخطوات التفصيلية:**
  1. **الخادم (Server Beacon):**
     - تشغيل خدمة إعلان Bonjour بمسمى الخدمة `_anpos._tcp`.
     - الرد التلقائي على نداءات UDP Broadcast على المنفذ `3001` وإرجاع مصفوفة من IP والمنفذ واسم المتجر.
  2. **العميل (Client Discovery Scanner):**
     - عند فتح البرنامج في وضع العميل، يظهر زر "البحث عن السيرفر في الشبكة".
     - البحث عن إشارات Bonjour / UDP وعرض قائمة بالخوادم المتاحة بنقرة واحدة ("خادم الكاشير الرئيسي - 192.168.1.100").
     - توفير حقل إدخال يدوي بديل لـ IP والمنفذ للشبكات المعزولة.
  3. **بروتوكول الاقتران الآمن (Pairing Handshake):**
     - يطلب العميل الاقتران بإرسال بصمة العتاد (Hardware Fingerprint) واسم الجهاز.
     - يظهر إشعار فوري في حاسوب الخادم للموافقة أو إدخال رمز PIN المكون من 6 أرقام.
     - عند الموافقة، يُصدر الخادم `Session Token` طويل الأمد يُحفظ في مخزن العميل الآمن.

---

### المرحلة 3: التحديثات المباشرة عبر WebSockets (Live Real-Time Event Bus) [مكتملة ومدمجة ✅]
* **الهدف:** إشعار الأجهزة الطرفية بالتغييرات فور حدوثها بالمللي ثانية دون الحاجة لـ Polling متكرر.
* **الخطوات المنجزة بالتفصيل:**
  1. دمج مسار WebSocket في خادم Fastify: `ws://[SERVER_IP]:3000/api/events` متوافق تماماً مع RFC 6455 دون أي مكتبات خارجية.
  2. دعم بديل تدفق الأحداث التلقائي Server-Sent Events (SSE) عبر `GET /api/events/sse` ومسار بث الأحداث `POST /api/events/emit`.
  3. إرسال إشعارات فورية متسلسلة عند وقوع الأحداث:
     - `product:updated`: تحديث السعر، الاسم، أو الصنف فورياً في شاشات الكاشير.
     - `stock:changed`: تحديث رصيد المخزون اللحظي لتفادي البيع بالسالب.
     - `order:suspended`: مشاركة الفواتير المعلقة عبر الصالة بين أجهزة الكاشير.
     - `customer:updated`: تحديث ديون ورصيد العملاء فور تسجيل أي دفعة.
     - `table:updated`: ربط تلقائي بـ `notifyTableChange` في قاعدة بيانات SQLite.
  4. محرك عميل `realtimeEventBus.ts` مع إعادة اتصال ذكية وتراجع أسي (Exponential Backoff: 1s -> 15s) ونبضات حيوية (Heartbeat) كل 20 ثانية لحساب Latency.
  5. شارة حالة تفاعلية نابضة (`RealtimeSyncBadge.tsx`) في كافة الأشرطة العلوية (🟢 تزامن مباشر / 🔴 جارٍ إعادة الاتصال / ⚙️ الخادم الرئيسي).

---

### المرحلة 4: محرك الصمود دون اتصال (Offline-First Outbox Engine) [مكتملة ومدمجة ✅]
* **الهدف:** ضمان استمرار الكاشير العميل في البيع وطباعة الفواتير حتى في حال انقطاع كابل الشبكة أو إعادة تشغيل الخادم.
* **الخطوات المنجزة بالتفصيل:**
  1. **التخزين المؤقت المحلي (Local Offline Read Replica - `offlineReplica.ts`):**
     - يحتفظ العميل بنسخة محلية فائقة السرعة (0ms) من جداول المنتجات، الباركودات، التصنيفات، العبوات، والعملاء عبر IndexedDB (مع مخزن ذاكرة احتياطي).
     - دعم التصفية، البحث الفوري، الترتيب، والتصفح لضمان عدم توقف عمليات البيع نهائياً.
  2. **طابور المعاملات المعلقة (Outbox Queue Table - `offlineOutbox.ts` & SQLite):**
     - إنشاء جدول `offline_sync_outbox` في SQLite وطابور IndexedDB لتسجيل:
       - معرف العملية (UUID Idempotency Key)، الكيان والعملية (`sales:create`, `payments:create`, `crud:update`)، البيانات كاملة بصيغة JSON، وقت الإنشاء، والحالة وعدد المحاولات.
  3. **بوابة النقل الذكية وتدفق الكاشير دون انقطاع (`transportGateway.ts`):**
     - عند فشل الاتصال بالخادم، يتم تلقائياً حفظ السجلات محلياً وإدراجها في الـ Outbox وإرجاع `{ queuedOffline: true }` للمحافظة على استمرار الكاشير في البيع وطباعة الفاتورة دون أي توقف أو رسائل خطأ معرقلة.
  4. **عامل المعالجة التلقائية (Outbox Dispatcher Worker):**
     - فور عودة اتصال الـ LAN بالخادم (عبر `window.online` أو استيقاظ `realtimeEventBus`)، يقوم العامل بإرسال العمليات المجمعة دفعة واحدة عبر `POST /api/sync/push`.
     - تأكيد الخادم للمعاملات بنجاح تام، وتحديث السجلات وحذف المكتمل منها مع منع التكرار.
  5. **شارة الحالة التفاعلية المحسنة (`RealtimeSyncBadge.tsx`):**
     - عرض عداد العمليات المعلقة التفاعلي النابض، وإمكانية المزامنة اليدوية الفورية بنقرة واحدة من نافذة التشخيص.

---

### المرحلة 5: محرك المزامنة السحابية (Cloud Synchronization Engine)
* **الهدف:** رفع بيانات المتجر إلى السحابة المركزية وإدارتها عن بعد مع استهلاك أقل قدر ممكن من البيانات.
* **الخطوات التفصيلية:**
  1. **حصرية المزامنة عبر الخادم (Single Egress Master Node):**
     - يتولى حاسوب الخادم فقط المزامنة مع السحابة لمنع تعارض المصادر، بينما ترتبط أجهزة العميل بالخادم فقط.
  2. **سجل التغييرات المتزايد (Change Data Capture / CDC):**
     - تتبع كل تعديل أو إضافة عبر عمودي `sync_version` و `updated_at`.
     - رفع التغييرات التي حدثت منذ آخر `last_cloud_sync_timestamp`.
  3. **السحب السحابي الدوري (Cloud Pull):**
     - سحب تعديلات الأسعار أو المنتجات المضافة من لوحة تحكم الإدارة السحابية وتوزيعها محلياً على الخادم والعملاء.
  4. **إدارة انقطاع الإنترنت:**
     - نظام الصمود الصامت: انقطاع الإنترنت لا يظهر أي أخطاء للمحاسب ولا يوقف أي شاشة، بل يتحول مؤشر السحابة إلى اللون الرمادي مع مواصلة تخزين الفواتير محلياً حتى استعادة الإنترنت.

---

### المرحلة 6: واجهة الإعداد السريع، الصلاحيات، وضمان الأمان (Wizard, RBAC & Hardening)
* **الهدف:** توفير تجربة مستخدم سلسة لأصحاب المتاجر وتأمين الاتصال عبر الشبكة.
* **الخطوات التفصيلية:**
  1. **معالج التثبيت والبدء لأول مرة (Initial Setup Wizard):**
     - شاشة بداية ترحيبية تسأل:
       - 🔘 **هذا الجهاز هو الخادم الرئيسي (Master Server)**
       - 🔘 **هذا الجهاز هو نقطة كاشير إضافية (Client Terminal)**
     - الضبط التلقائي للمنافذ وإعدادات جدار الحماية والخدمات وفقاً للاختيار.
  2. **فصل الصلاحيات والأمان (Role-Based Access Control):**
     - قفل الوصول إلى شاشات الإعدادات المحاسبية المتقدمة والنسخ الاحتياطي على أجهزة العملاء إلا برقم سري للمدير أو بإذن من الخادم.
     - تشفير رمز الجلسة `Session Token` باستخدام مفتاح عتاد الجهاز.
  3. **اختبارات الاستقرار ومحاكاة الأعطال (Chaos & Load Testing):**
     - اختبار فصل راوتر الشبكة أثناء عملية الدفع والتحقق من حفظ الفاتورة في الـ Outbox.
     - اختبار إجراء 50 عملية بيع في نفس الثانية من الخادم والعميل والتحقق من سلامة الأرصدة والمخزون.

---

## 6. مصفوفة حالات الفشل والحلول الوقائية (Failure Scenarios Matrix)

| سيناريو الخلل أو الانقطاع | التأثير على نقطة البيع | الإجراء الوقائي والحل المدمج في المعمارية |
| :--- | :--- | :--- |
| **انقطاع الإنترنت الخارجي بالكامل** | لا يوجد أي تأثير على البيع المحلي | يستمر الخادم والعميل بالعمل 100% عبر شبكة LAN مع تأجيل المزامنة السحابية |
| **توقف راوتر Wi-Fi أو كابل LAN** | تعذر اتصال العميل بالخادم | ينتقل العميل فوراً لوضع Offline Outbox، يكمل عمليات البيع ويطبع الفواتير محلياً |
| **إعادة تشغيل حاسوب الخادم فجأة** | انقطاع لحظي لثوانٍ | يحتفظ العميل بالمبيعات في طابوره المحلي، وفور استيقاظ الخادم يتم تفريغ الفواتير تلقائياً |
| **تلف القرص الصلب لحاسوب الخادم** | توقف الخادم المحلي | استعادة فورية لآخر نسخة احتياطية من السحابة المركزية وتعيين كمبيوتر العميل كخادم مؤقت |
| **محاولة اختراق أو دخول جهاز غريب للشبكة** | محاولة طلب بيانات المتجر | رفض الطلبات غير المرخصة (401/403)، إلزامية الاقتران بـ PIN من شاشة الخادم مع Whitelist IP |

---

## 7. الجدول الزمني للتنفيذ المقترح

```
الأسبوع 1: [المرحلة 1] تجريد طبقة نقل البيانات (Transport Gateway) وإعداد خيار Server/Client. [مكتملة ✅]
الأسبوع 2: [المرحلة 2] بروتوكول الاكتشاف التلقائي (mDNS/UDP) والاقتران المشفر بحاسوب الخادم. [مكتملة ✅]
الأسبوع 3: [المرحلة 3] محرك الأحداث اللحظية المباشرة (WebSockets Event Bus). [مكتملة ✅]
الأسبوع 4: [المرحلة 4] محرك العمليات المحلية دون اتصال (Offline-First Outbox Resilience). [مكتملة ✅]
الأسبوع 5: [المرحلة 5] محرك المزامنة السحابية المزدوج (Cloud Sync Engine & CDC).
الأسبوع 6: [المرحلة 6] معالج الإعداد السريع، تدقيق الأمان، واختبارات التحمل والإطلاق.
```
