# معمارية وهندسة شاشة إدارة المفضلة والعبوات السريعة (Favorites & Quick Packs Architecture)
> **AN POS System — Enterprise Favorites & Terminal 5 Quick Packs Architecture**

---

## 1. التحليل والملخص التنفيذي (Executive Summary & Problem Statement)

تعتبر شاشة إدارة المفضلة والعبوات (`/favorites`) مركز التحكم المخصص لتهيئة كاشير نقطة البيع المتقدمة (تصميم 5 - Terminal POS). عند فحص الكود الحالي، تبيّن أن ملف:
- **`src/features/favorites/FavoritesPage.tsx`** يحتوي على **1,848 سطراً برمجياً** مكدسة بالكامل في ملف أحادي واحد (Monolith).

### المشكلات الهندسية المكتشفة في الملف:
1. **تضخم هائل في واجهات المستخدم المضمنة (Excessive Embedded JSX)**:
   - يحتوي الملف على 4 نوافذ منبثقة (Modals) كبيرة مضمنة جميعها داخل نفس المكون:
     - **Modal 1: نافذة إضافة وتعديل التصنيفات** (100 سطر).
     - **Modal 2: نافذة تصفح وإضافة العبوات والمنتجات** (184 سطر).
     - **Modal 3: معالج إنشاء العبوات السريعة من 7 خطوات (Quick Pack Wizard)** (320 سطر).
     - **Modal 4: نافذة تعديل بيانات وأسعار العبوة (Edit Pack Modal)** (252 سطر).
   - هذا يجعل ما يقارب **856 سطراً** من الكود عبارة عن حوارات منبثقة تعيق القراءة وتزيد من تعقيد شجرة الرندر (Render Tree).
2. **تداخل منطق الأعمال والاتصال بقواعد البيانات (Coupled Business Logic & Dual-Persistence)**:
   - خوارزميات حفظ وتعديل العبوات في كل من `SQLite` عبر (Electron IPC API) و `Dexie` التابع للمتصفح و `Zustand store` مدمجة بشكل مكرر ومباشر داخل معالجات الأحداث (Event Handlers).
3. **تشتت إدارة الحالة (State Proliferation)**:
   - يحتوي المكون على أكثر من 30 متغير حالة `useState` متداخلة تدير إدخالات النماذج، أخطاء التحقق، ومؤشرات التحميل، مما يؤدي إلى إعادة رندر (Re-render) لكامل الصفحة مع كل نقرة زر أو ضغطة مفتاح.

---

## 2. المعمارية النمطية المستهدفة (`src/features/favorites/`)

سنقوم بتفكيك وحدة المفضلة وفق مبادئ **Clean Architecture** و **Single Responsibility Principle (SRP)** إلى بنية واضحة المعالم:

```
src/features/favorites/
├── types.ts                                       # العقود والأنواع (QuickPackFormData, EditPackFormData, ModalProps)
├── constants/
│   └── favoriteVisuals.ts                         # ثوابت الأيقونات (ICON_MAP) وخيارات الألوان (COLOR_OPTIONS)
├── services/
│   └── favoritePackService.ts                     # محرك الحفظ والتعديل المزدوج للعبوات (Electron IPC + Dexie) وحساب الأسعار التلقائية
├── hooks/
│   ├── useFavoritesData.ts                        # خطاف استعلامات العبوات والمنتجات من قاعدة البيانات مع التخزين المؤقت
│   ├── useFavoritesCategoryFilter.ts              # خطاف إدارة التصنيف النشط والبحث وتصفية بنود المفضلة
│   ├── useCategoryFormModal.ts                    # خطاف إدارة حالة ونموذج إضافة/تعديل التصنيفات
│   ├── useQuickPackModal.ts                       # خطاف معالج إنشاء العبوة السريعة من 7 خطوات وحسابات الأسعار
│   └── useEditPackModal.ts                        # خطاف إدارة حالة وتعديل أسعار وبيانات العبوة الحالية
├── components/
│   ├── FavoritesHeader.tsx                        # ترويسة الصفحة، مفتاح تفعيل المفضلة لتصميم 5، وأزرار الإجراءات
│   ├── FavoritesStatsBar.tsx                      # شريط الإحصائيات الأربعة (التصنيفات، عبوات المفضلة، المنتجات، عبوات المخزن)
│   ├── FavoriteCategoriesSidebar.tsx              # عمود قائمة التصنيفات (تصنيف "الكل" + التصنيفات المخصصة مع الألوان والأيقونات)
│   ├── FavoritePacksGrid.tsx                      # شبكة عرض بطاقات العبوات المفضلة وشريط البحث وحالة الفراغ
│   └── FavoritePackCard.tsx                       # بطاقة العبوة المفردة مع الأسعار وعدد القطع وأزرار التعديل والحذف
├── modals/
│   ├── CategoryFormModal.tsx                      # نافذة إنشاء وتعديل تصنيفات المفضلة (الأيقونة، اللون، الاسم)
│   ├── AddFavoriteItemsModal.tsx                  # نافذة إضافة عبوات من المخزن أو تحويل منتجات التجزئة إلى كراتين
│   ├── QuickPackModal.tsx                         # معالج إنشاء العبوة السريعة (7 خطوات تفصيلية لحساب القطع والسعر والخصم)
│   └── EditFavoritePackModal.tsx                  # نافذة تعديل بيانات وسعر عبوة المفضلة
├── store/
│   └── useFavoritesStore.ts                       # متجر Zustand القائم لإدارة فئات وبنود المفضلة (محفوظ 100%)
├── __tests__/
│   └── favoritePackService.test.ts                # اختبارات الوحدة لحساب الأسعار التلقائية وبناء العبوات وتسميتها
├── FavoritesPage.tsx                              # المنسق الرئيسي الخفيف (Orchestrator < 140 سطر)
└── index.ts                                       # نقطة التصدير الموحدة للوحدة
```

---

## 3. تفصيل طبقات المعمارية والمسؤوليات

### 3.1 طبقة العقود والأنواع (`types.ts`)
- إعادة تصدير `FavoriteCategory` و `FavoriteItem` من المتجر.
- تعريف عقود نماذج الإدخال:
  - `CategoryFormData`: اسم التصنيف، الأيقونة، واللون.
  - `QuickPackFormData`: المنتج الأساسي، عدد القطع، مسمى الوحدة، اسم العبوة، السعر، الباركود، والتصنيف المستهدف.
  - `EditPackFormData`: بيانات العبوة أثناء التعديل وأسعار الحبة والتصنيف.

### 3.2 طبقة الثوابت المرئية (`constants/favoriteVisuals.ts`)
- عزل مصفوفة خيارات الألوان الـ 8 (`COLOR_OPTIONS`).
- عزل خريطة الأيقونات (`ICON_MAP`) المتاحة لتصنيفات المفضلة.

### 3.3 طبقة الخدمات والمحركات النقية (`services/favoritePackService.ts`)
- `calculateQuickPackPrice(baseUnitPrice, piecesCount)`: حساب السعر التلقائي للعبوة بناءً على سعر التجزئة وعدد القطع.
- `buildQuickPackEntity(params)`: بناء كيان `PackEntity` نظيف وجاهز للحفظ.
- `savePackDualPersistence(packData, isUpdate)`: دالة موحدة تتولى الحفظ في `SQLite` (عبر Electron API) مع الرجوع التلقائي إلى `Dexie` وتحديث الكاش دون تكرار الكود.

### 3.4 طبقة الخطافات المخصصة (`hooks/`)
1. **`useFavoritesData`**:
   - إدارة استعلامات React Query لجلب العبوات (`packs`) والمنتجات (`products`) من Electron و Dexie.
2. **`useFavoritesCategoryFilter`**:
   - إدارة التصنيف المحدد (`selectedCatId`)، استعلام البحث (`searchQuery`)، ومصفوفة البنود المعروضة (`displayedItems`).
3. **`useCategoryFormModal`**:
   - إدارة حالة فتح النافذة، التصنيف الجاري تعديله، إدخالات الاسم والأيقونة واللون، وعملية الحفظ والتحديث والحذف.
4. **`useQuickPackModal`**:
   - إدارة معالج الـ 7 خطوات: اختيار منتج التجزئة، عدد القطع، مسمى الوحدة، توليد الاسم المقترح، حساب السعر التلقائي أو المخصص، وتخزين العبوة في المتجر وقاعدة البيانات.
5. **`useEditPackModal`**:
   - فتح نافذة تعديل العبوة، ربط المنتج الأصلي التابع لها، حساب السعر التلقائي، وحفظ التعديلات في متجر المفضلة وجدول العبوات.

### 3.5 طبقة النوافذ المنبثقة المستقلة (`modals/`)
- عزل كل نافذة من النوافذ الأربع في ملف مستقل لا يتجاوز 150-250 سطراً مع فصل مدخلات النموذج، مما يمنع إعادة رندر الصفحة الرئيسية عند الكتابة في حقول الإدخال.

### 3.6 المنسق الرئيسي (`FavoritesPage.tsx`)
- تجميع الطبقات في أقل من 140 سطر برمجي نظيف ومقروء بالكامل، مما يوفر تجربة صيانة وتطوير استثنائية.

---

## 4. خطة الاختبار الآلي ومراقبة الجودة (Test Strategy)
- كتابة اختبارات وحدة شاملة في `src/features/favorites/__tests__/favoritePackService.test.ts` لتغطية:
  1. الحساب التلقائي لأسعار العبوات بحسب سعر الحبة وعدد القطع.
  2. توليد الأسماء المعيارية للكراتين والعبوات.
  3. بناء بنود العبوة والتحقق من صحة المدخلات.
- التحقق من فحص `TypeScript` لضمان خلو وحدة المفضلة بالكامل من أي أخطاء نوعية.
